# 壁追加時トリム不良

## 問題
壁追加時、先に追加されている直交する壁が正しく短くならない

## 実行前状況
初期状態から、x1,x2,Y1, Y2の構造芯
X2から左100に壁1を追加
Y1から上に中心線1追加

## 実行する操作
追加した中心線1から下100に壁2追加

## 問題のある結果
壁1の下がカットされ__|なる。

## 希望する結果
壁1の上をカットして__
　　　　　　　　     |する。

## 修正方針

それぞれ、できあがる壁として
   A:_| V| B:|_
 中心線1--+-----
     _    　_
     C: |  D:|

本件ではC:が正解

## 修正案（実装手順）

問題箇所は `app/src/core.js` の `trimIntersectingWalls`（既に指摘された 669-682 行付近）です。
現状は intersection を検出した後、単純に両壁の最寄り端を `_trimWallEnd` でスナップしていますが、
四象限（A/B/C/D）の判定を明示して「どちらの端を切るべきか」を決定する必要があります。

判定の方針（要点）:
- 新規壁と既存壁の交点を (vx, hy) とする。
- 既存の垂直壁 `v` については `v.coord1` / `v.coord2`（始点/終点の座標）と比較し、
  - 交点が `v` の延長線の外側（始点よりもさらに上／終点よりもさらに下）にある場合は明示的にその側の端をトリムする。
  - 交点が `v` の範囲内にある（両端の間）場合は「最も近い端」をトリムする。
- 水平壁 `h` についても同様に `h.coord1` / `h.coord2` と比較して左端/右端を決定する。

擬似コード（`trimIntersectingWalls` 内の該当箇所を置き換える想定）:

```js
// intersection は既に検出済み: vx, hy, v, h
const vMin = Math.min(v.coord1, v.coord2);
const vMax = Math.max(v.coord1, v.coord2);
if (hy <= vMin) {
  // 交点は v の "上側"（y が小さい側）にある → 始点をトリム
  v.clStart = h.axisCL; v.startOffset = h.axisOffset;
} else if (hy >= vMax) {
  // 交点は v の "下側"（y が大きい側）にある → 終点をトリム
  v.clEnd = h.axisCL; v.endOffset = h.axisOffset;
} else {
  // 交点は v の内部 → 最も近い端をトリム
  _trimWallEnd(v, hy, h.axisCL, h.axisOffset);
}

const hMin = Math.min(h.coord1, h.coord2);
const hMax = Math.max(h.coord1, h.coord2);
if (vx <= hMin) {
  // 交点は h の左側 → 始点をトリム
  h.clStart = v.axisCL; h.startOffset = v.axisOffset;
} else if (vx >= hMax) {
  // 交点は h の右側 → 終点をトリム
  h.clEnd = v.axisCL; h.endOffset = v.axisOffset;
} else {
  _trimWallEnd(h, vx, v.axisCL, v.axisOffset);
}
```

注意点:
- 座標系は y が下向き正なので「上側 = 値が小さい」「下側 = 値が大きい」です。`Math.min`/`Math.max` を使い、`coord1/coord2` の順序に依存しないようにすること。
- `coord1/coord2` は `clStart.value + startOffset` / `clEnd.value + endOffset` を参照する computed なので、`_trimWallEnd` の呼び出し前後で一貫した値を使うこと。
- このロジックを入れると、質問の図で要求した `C` パターン（既存壁の上を切る）が正しく選ばれるはずです。

実装後は `app` ディレクトリで簡易再現スクリプト（既に私が用意した `tmp_trim_test.mjs` のようなもの）で検証してください。
